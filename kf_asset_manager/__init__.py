"""KF Asset Manager — local master asset database for Karen Fabrics.

The single source of truth for every downstream system (Shopify, Kickflip,
mockups, search). Scan any folder, review and edit metadata with permanent manual
overrides, export a manifest + crosswalk.
"""
__version__ = "0.1.0"
